{
  "name": "karviyam-root",
  "version": "1.0.0",
  "private": true,
  "description": "Karviyam E-Commerce Web Application Root Workspace",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "postinstall": "npm --prefix frontend install && npm --prefix backend install",
    "build": "npm --prefix frontend install && npm --prefix frontend run build && node scripts/post-build.js",
    "dev": "npm --prefix frontend run dev",
    "start": "npm --prefix backend start"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3"
  }
}
